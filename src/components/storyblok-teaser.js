import React from "react"

const Teaser = ({ blok }) => (
  <section className="container max-w-3xl m-auto py-8">
    <p className="text-2xl">{blok.headline}</p>
  </section>
)

export default Teaser
